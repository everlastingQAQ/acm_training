#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

void solve ()
{
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        int t;
        cin >> t;
    }
    cout << "?" << ' ' << 1 << ' ' << n << endl;
    int t1, t2;
    cin >> t1;
    cout << "?" << ' ' << n << ' ' << 1 << endl;
    cin >> t2;
    if (t2 >= n - 1 && t1 >= n - 1) {
        cout << "!" << ' ' << 'B' << endl;
    }else if ()
}

int main ()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int _ = 1;
    cin >> _;
    while (_--) {
        solve();
    }
    return 0;
} 