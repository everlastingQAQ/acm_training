#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int t;
    cin >> t;

    for (int i = 0; i < t; i++) {
        int n, m, x, y;
        cin >> n >> m >> x >> y;
        for (int j = 0; j < n; j++) {
            int a;
            cin >> a;
        }
        for (int j = 0; j < m; j++) {
            int a;
            cin >> a;
        }
        cout << n + m << endl;
    }

    return 0;
}