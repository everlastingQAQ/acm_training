#include <bits/stdc++.h>
using namespace std;
int MAXN = 1002;
int n, k;

int main ()
{
    long long result[MAXN] = {};
    cin >> n >> k;

    long long a[MAXN] = {};
    for (int i = 1; i <= n; i++) {
        a[i] = i;
    }

    
}